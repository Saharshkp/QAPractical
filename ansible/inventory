all:
    children:
        manager:
            hosts:
                swarmmanager:

        worker:
            hosts:
                swarmworker:
    
    vars: 
        ansible_user: spacebook
        ansible_ssh_private_key_file: "~/.ssh/id_rsa"